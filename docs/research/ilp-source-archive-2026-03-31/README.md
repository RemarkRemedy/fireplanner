This folder preserves the temporary research artifacts that were downloaded into `/tmp`
during the 2026-03-31 ILP insurer benchmark / ETF proxy / annual-fee extraction pass.

Files include:
- raw PDFs pulled from insurer sites
- HTML snapshots used for scraping
- fee-cache directories created from downloaded source PDFs
- a few intermediate artifacts used to debug extraction gaps

See `manifest.tsv` for the saved file list, byte sizes, and SHA-256 hashes for file entries.
